usage: main.py [-h] --train-data-path TRAIN_DATA_PATH [--use-dp]

Preprocess Adult data and train deep model

optional arguments:
  -h, --help            show this help message and exit
  --train-data-path TRAIN_DATA_PATH
                        Path to Adult training data (CSV)
  --use-dp              Use DP-SGD